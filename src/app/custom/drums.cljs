(ns app.custom.drums
  "User custom drum models, physical percussion presets, and kit sound design.")

;; Custom Drum Models Catalog
;;
;; Define your custom drum models and percussion voices in `user-drums`.
;; For parameter schemas, baseline acoustic kits, and sound design references,
;; see the core catalog in: src/app/lib/drums.cljs (`app.lib.drums/core-drums`).

(def user-drums
  {})
