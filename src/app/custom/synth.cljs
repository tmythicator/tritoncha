(ns app.custom.synth
  "User custom synthesizers and sound design presets.")

;; Custom Synthesizers Catalog
;;
;; Define your custom synthesizer voice patches and sound design presets in `user-synths`.
;; For oscillator types, filter modes, ADSR envelope parameters, and baseline voice references,
;; see the core catalog in: src/app/lib/synth.cljs (`app.lib.synth/core-synths`).

(def user-synths
  {})
