(ns app.custom.scenes
  "User-defined 3D scenes, procedural geometries and shader effects.")

;; Custom 3D Scenes Catalog
;;
;; Define your custom 3D WebGL scenes, procedural geometries, and animations in `user-scenes`.
;; For geometry schemas, camera setups, material parameters, and built-in scene presets,
;; see the core catalog in: src/app/lib/scenes.cljs (`app.lib.scenes/core-scenes`).

(def user-scenes
  {})
