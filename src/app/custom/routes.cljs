(ns app.custom.routes
  "User custom audio routing topologies and DSP bus graphs for Tritoncha.")

;; Custom Audio Routing Topologies
;;
;; Define modular DSP bus effect chains, processor graphs, and bus topologies in `user-routes`.
;; For routing graph schemas, processor parameter specifications, and built-in topology presets,
;; see the core catalog in: src/app/lib/routes.cljs (`app.lib.routes/core-routes`).

(def user-routes
  {})
